---
description: HeroUI Native — docs (@Docs, CLI) and Expo wiring for Core (and legacy mobile)
globs: apps/core/**/*.{ts,tsx},apps/mobile/**/*.{ts,tsx}
alwaysApply: false
---

# HeroUI Native (Core app)

## Docs for the AI

1. **Local (recommended):** Run from repo root:  
   `npx heroui-cli@latest agents-md --native --output AGENTS.md`  
   Docs land in `.heroui-docs/native/` (gitignored); the machine-readable index is in **`AGENTS.md`** at repo root.

2. **Cursor `@Docs`:** Attach **`https://heroui.com/native/llms-full.txt`** in chat for full upstream docs. Shorter index: **`https://heroui.com/native/llms.txt`**.

3. Optional analytics opt-out for the CLI: `HEROUI_ANALYTICS_DISABLED=1`.

## This codebase

- **`apps/core/global.css`** — `@import 'tailwindcss'`, `uniwind`, `heroui-native/styles`, `@source` for `heroui-native/lib`.
- **`apps/core/metro.config.js`** — **`withUniwindConfig`** is the outermost Metro wrapper (`cssEntryFile: './global.css'`).
- **`apps/core/app/_layout.tsx`** — **`HeroUINativeProvider`** inside **`GestureHandlerRootView`**; **`Uniwind.setTheme(resolvedTheme)`** aligned with **`ThemePreferenceProvider`** (RN `StyleSheet` / icons may still use **`useAppColors()`**).
- Prefer **`heroui-native`** components + Uniwind **`className`** where the library expects it; use **`~/lib/theme`** (`useAppColors`) for RN `StyleSheet` / icons where HeroUI CSS tokens do not apply.
