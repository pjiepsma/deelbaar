# Copy to: apps/mobile/.env  (same folder as this file)
# Never commit .env — it is gitignored.

# Payload CMS API (mobile talks to Next on port 4000 when you run pnpm dev:cms)
# Emulator: http://localhost:4000 is OK (rewritten to 10.0.2.2 on Android in dev).
# Physical device: use your PC's LAN IP, e.g. http://192.168.0.63:4000
EXPO_PUBLIC_PAYLOAD_URL=http://localhost:4000

# Mapbox — required for maps (@rnmapbox/maps)
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.eyJ1IjoieW91ci11c2VybmFtZSIsImEiOiJjbG...

# Optional: Gemini (book spine / vision)
# EXPO_PUBLIC_GEMINI_API_KEY=

# Optional: Stripe Connect onboarding
# EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXX
